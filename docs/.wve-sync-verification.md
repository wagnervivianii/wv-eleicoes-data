# Temporary synchronization verification

This branch-only file exists solely to let the WVE worker publish an authoritative validation report. This PR MUST NOT BE MERGED and must be closed without merge after verification.

The orchestrator must verify that the canonical checkout at `/home/devw/wvtechsolutions/projects/wv-eleicoes/wv-eleicoes-data` is clean and not diverged before touching it. If dirty or diverged, block without overwriting. After successful validation, report REVIEW_READY and record the canonical HEAD and required files.
