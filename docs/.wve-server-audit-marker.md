# Temporary server audit evidence

This branch exists only to publish audit evidence. This PR will be closed without merge.

## Audit result — 2026-09-12

Target SSH alias: `wvprod`. Future project root: `/opt/wvtechsolutions/projects/wv-eleicoes`.

`SAFE_TO_PROVISION=no`

Provisioning is blocked by unavailable server evidence. This result does not establish that the server itself is unsafe or unavailable from other environments.

### Observed local evidence

- SSH client available at `/usr/bin/ssh`.
- Effective SSH summary (`ssh -G wvprod`, filtered to these three fields): hostname `wvprod`, user `devw`, port `22`.
- `SSH_FAILED exit=255`: `Could not resolve hostname wvprod: Name or service not known`.
- Connection used batch mode, a 10-second timeout, one attempt, strict host-key checking, disabled host-key updates, no connection multiplexing, no agent forwarding, and no configured forwarding. No known-host entry was created or updated by this attempt.
- Remote commands were not executed because hostname resolution failed. No server mutation was performed.

### Unavailable remote checks

All checks below were blocked by the same SSH resolution failure; no permission denial or resource absence is inferred.

| Check | Diagnostic marker / result |
| --- | --- |
| Connectivity and remote username/hostname | `SSH_FAILED`; remote identity unknown |
| `/opt/wvtechsolutions` paths, maximum depth 3 | `TREE_UNAVAILABLE` |
| Future WV Eleições root existence | `PROJECT_ROOT_UNAVAILABLE`; existence unknown |
| `deploy.sh` existence, owner, mode, structural command names | `DEPLOY_AUDIT_UNAVAILABLE` |
| Docker container names/images/ports, networks, named volumes | `DOCKER_UNAVAILABLE`; daemon permissions untested |
| PostgreSQL container/service/version/bind ports/mount destinations | `POSTGRES_RUNTIME_UNAVAILABLE` |
| Non-template database names and existence/attributes of `postgres` and WV Eleições roles | `POSTGRES_CATALOG_UNAVAILABLE` |
| Relevant systemd unit names | `SYSTEMD_UNAVAILABLE` |
| Disk and memory | `CAPACITY_UNAVAILABLE` |

### Blockers and recommended method

The audit execution environment cannot resolve `wvprod`; the effective hostname remains the alias itself. Server identity, existing workloads, project-path occupancy, PostgreSQL configuration and roles, and capacity remain unverified.

The orchestrator should rerun this read-only audit from an environment with the intended SSH alias configuration and hostname resolution, using existing authorized authentication and host trust. Do not infer a replacement address or provision against an unverified host. Missing permissions in subsequent checks should be reported individually and remaining checks continued. Keep provisioning blocked until the required evidence is available and reviewed.

After a successful audit, use the existing repository-managed deployment process through the orchestrator for any separately authorized provisioning under the future project root. The deployment mechanism and any reuse of PostgreSQL must be selected from verified server evidence; neither can be recommended concretely from this attempt.

No environment variables, credential files, private keys, password hashes, or potentially secret deployment-script contents were printed. The only workspace change is this temporary evidence marker. No application code changed; application tests and migration checks were not applicable.
